@model IEnumerable<UserApplication.Models.TeacherInSubject>

@{
    ViewBag.Title = "TeachersSubject";
    Layout = "~/Views/Shared/_LogOutPage.cshtml";
}
<h1 align="middle"><b>VIDHYA NIKETAN</b></h1>
<h2 align="middle"><i>THE INFINITE CAMPUS</i></h2>
<center><img src=" http://www.vidyaniketansaket.in/image/f-logo.png" width=" 90" height=" 90" /></center>

<h2>TeachersSubject</h2>

<p>
    @Html.ActionLink(" ", " ")
</p>
<table class="table table-bordered table-responsive table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.User.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Subject.SubjectName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subject.SubjectName)
            </td>
            <td>
                @Html.ActionLink("Courses of student", "StudentCourse", new { id = item.UserId })
                @*@Html.ActionLink("Edit", "Edit", new { id=item.TeacherInSubjectId }) |
                    @Html.ActionLink("Details", "Details", new { id=item.TeacherInSubjectId }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.TeacherInSubjectId })*@
            </td>
        </tr>
    }

</table>
<p> @Html.Partial("_BackToListButtonForStudent")</p>
